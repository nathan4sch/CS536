{
  "backend": "gloo",
  "benchmark_warmup_iterations": 2,
  "benchmark_timed_iterations": 5,
  "fixed_rank_for_allgather_message_sweep": 8,
  "fixed_message_size_for_allgather_rank_sweep_bytes": 1048576,
  "message_sizes_bytes": [
    1024,
    4096,
    16384,
    65536,
    262144,
    1048576,
    4194304,
    8388608
  ],
  "rank_counts": [
    2,
    4,
    8
  ],
  "allgather_algorithms": [
    "ring",
    "recursive_doubling",
    "swing"
  ],
  "expected_plots": [
    "allgather_vs_message_size",
    "allgather_vs_ranks"
  ],
  "notes": [
    "The message-size sweep fixes the rank count at 8.",
    "The rank-scaling sweep fixes the message size at 1 MB per rank.",
    "The largest message size is capped at 8 MB per rank to keep memory usage reasonable.",
    "Recursive doubling assumes a power-of-two number of ranks, so the default rank counts stay at 2, 4, and 8."
  ]
}
